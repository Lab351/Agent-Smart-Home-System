name = "commit-notify"
main = "index.js"
compatibility_date = "2024-01-01"

# Secrets are NOT stored here — set them with:
#   wrangler secret put NOTIFY_WEBHOOK_URL
#   wrangler secret put GITHUB_WEBHOOK_SECRET   (optional)
